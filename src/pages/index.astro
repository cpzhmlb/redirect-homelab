---
import "../styles/style.css";
import ServiceLink from "../components/ServiceLink.astro";
import LiquidSelect from "../components/LiquidSelect.astro";
import services from "../data/services.json";

const currentNetwork = typeof localStorage !== 'undefined' 
  ? localStorage.getItem('useNetwork') || 'public' 
  : 'public';
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Redirect Homelab</title>
    <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
  </head>
  <body>
    <div class="app-container">
      <header
        class="page-head"
        style="display:flex;align-items:center;justify-content:space-between;gap:12px;"
      >
        <h1 class="header">Redirect Homelab</h1>
        <div style="display:flex;align-items:center;gap:8px;">
          <LiquidSelect currentNetwork={currentNetwork} client:load />
        </div>
      </header>

      <section class="card">
        <p>Public Services:</p>

        <div class="services-list public-services-list">
          {services.public.map((service) => <ServiceLink {...service} />)}
        </div>
      </section>

      <section class="card">
        <p>Private Services:</p>

        <div class="services-list private-services-list">
          {services.private.map((service) => <ServiceLink {...service} />)}
        </div>
      </section>
    </div>

    <style>
      .app-container {
        padding: 0;
      }

      .header {
        margin-bottom: 4px;
      }

      .card {
        margin-bottom: 28px;
      }

      .services-list {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 16px;
      }

      .public-services-list {
        margin-top: 6px;
      }
      .private-services-list {
        margin-top: 6px;
      }

      .app-footer {
        margin-top: 8px;
      }
    </style>
  </body>
</html>
